import 'package:flutter/material.dart';

const kPlaylistText = TextStyle(
  color: Colors.white,
  fontSize: 17.0,
  letterSpacing: 3.0,
);

const kDarkShadow = BoxShadow(
    color: Colors.black, offset: Offset(4, 4), blurRadius: 15, spreadRadius: 1);
